<Page
    x:Class="PokeDex.ExtendedSplash"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PokeDex"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
        <Grid.Background>
            <ImageBrush x:Name="extendedSplashImage" ImageSource="Assets/pokeball.jpg"
                 Stretch="UniformToFill"  />
        </Grid.Background>
        <Canvas >
            <ProgressRing Name="splashProgressRing" IsActive="False" Width="20" HorizontalAlignment="Center"/>
        </Canvas>
        <Button x:Name="DismissSplash" Content="Start"
                    BorderBrush="white"
                    Background="white"
                    Style="{StaticResource NavigationViewTitleHeaderContentControlTextStyle}" HorizontalAlignment="Right" Click="DismissSplashButton_Click"
                    Canvas.Left="809" Margin="0,0,0,0" Canvas.Top="465" />
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Bottom"/>
    </Grid>
</Page>
